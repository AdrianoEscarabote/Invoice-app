import { render } from "@testing-library/react";
import HeaderLoginSignup from ".";

describe("HeaderLoginSignup", () => {
  it("should render correctly", () => {
    render(<HeaderLoginSignup />);
  });
});
