import { Meta, StoryObj } from "@storybook/react";
import DeletePrompt from ".";

export default {
  title: "components/DeletePrompt",
  component: DeletePrompt,
} as Meta;

export const Primary: StoryObj = {};
