import { Meta, StoryObj } from "@storybook/react";
import InvoiceButton from "./";

export default {
  title: "components/InvoiceButton",
  component: InvoiceButton,
  tags: ["autodocs"],
} as Meta;

export const Primary: StoryObj = {};
