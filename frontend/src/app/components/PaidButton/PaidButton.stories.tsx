import { Meta, StoryObj } from "@storybook/react";
import PaidButton from "./";

export default {
  title: "components/PaidButton",
  component: PaidButton,
  tags: ["autodocs"],
} as Meta;

export const Primary: StoryObj = {};
